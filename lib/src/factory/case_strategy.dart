/// Enum for the different string case strategies.
enum CaseStrategy {
  camelCase,
  constantCase,
  sentenceCase,
  snakeCase,
  dotCase,
  paramCase,
  pathCase,
  pascalCase,
  headerCase,
  titleCase,
}
